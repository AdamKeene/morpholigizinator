  
*   **`CN` (Common Noun)**: Represents common noun phrases, allowing for modification by adjectives.
*   **`AdvVP` (Adverbial Verb Phrase)**: Allows for the modification of verb phrases by adverbs.
*   **`DetCN` (Determiner Common Noun)**: Combines a determiner with a common noun to form a noun phrase.
*   **`ModCN` (Modifier Common Noun)**: Modifies a common noun with an adjective phrase.
*   **`PositA` (Positive Adjective)**: Creates an adjective phrase from an adjective.

**Example Abstract Syntax Structure:**

```gf
abstract WordNetTranslatorV3 = WordNetLexiconV2 ** {
  flags startcat = S ;
  cat
    S ; NP ; VP ; AP ; Det ; CN ;
  fun
    -- Sentence structures
    Pred : NP -> VP -> S ;
    
    -- Verb Phrase structures
    UseV : V -> VP ;
    Compl : V2 -> NP -> VP ;
    AdvVP : VP -> Adv -> VP ;
    
    -- Noun Phrase structures
    DetCN : Det -> CN -> NP ;
    UseN : N -> NP ; -- Simplified NP from N
    
    -- Common Noun structures
    ModCN : AP -> CN -> CN ;
    UseN_CN : N -> CN ;
    
    -- Adjective Phrase structures
    PositA : A -> AP ;
    
    -- Determiners
    the_Det : Det ;
    a_Det : Det ;
    every_Det : Det ;
}
```

## 2. Updated Concrete Syntaxes (WordNetTranslatorV3Eng.gf, WordNetTranslatorV3Spa.gf)

The English and Spanish concrete syntaxes (`WordNetTranslatorV3Eng.gf` and `WordNetTranslatorV3Spa.gf`) have been updated to reflect the changes in the abstract syntax. They now include linearization rules for the new categories and functions, leveraging the RGL modules (`SyntaxEng`, `ParadigmsEng`, `SyntaxSpa`, `ParadigmsSpa`) to generate grammatically correct output in both languages.

## 3. Python Translation Function (`translate_text.py`)

A Python script, `translate_text.py`, has been developed to provide a convenient interface for translating text using the compiled GF grammar (`WordNetTranslatorV3.pgf`). This script utilizes the `gf` command-line tool to parse and linearize sentences.

**Key Features:**

*   **`GFTranslator` Class**: Initializes with the PGF file path and the WordNet database path.
*   **`translate_spa_to_eng(text)`**: Takes a Spanish sentence as input, parses it using the Spanish concrete syntax, and linearizes the resulting abstract syntax tree into English.
*   **`translate_eng_to_spa(text)`**: Takes an English sentence as input, parses it using the English concrete syntax, and linearizes the resulting abstract syntax tree into Spanish.
*   **Robustness**: Uses `subprocess` to call the `gf` command-line tool, providing better error handling and compatibility.

**Example Usage (from `translate_text.py`):**

```python
# Example translations
test_sentences_spa = [
    "la bomba atómica cede",
    "cada bomba atómica abandona abacá"
]

for sent in test_sentences_spa:
    results = translator.translate_spa_to_eng(sent)
    print(f"Results: {results}\n")
    
test_sentences_eng = [
    "the A-bomb abates",
    "every A-bomb abandons abaca"
]

for sent in test_sentences_eng:
    results = translator.translate_eng_to_spa(sent)
    print(f"Results: {results}\n")
```

## 4. Verification and Testing

The expanded system was successfully compiled into `WordNetTranslatorV3.pgf` and tested with various sentences, demonstrating its ability to handle more complex structures.

**Test Cases:**

| Abstract Tree | Spanish Linearization | English Linearization |
| :--- | :--- | :--- |
| `Pred (UseN a_bomb_N) (UseV abate_2_V)` | `la bomba atómica cede` | `the A-bomb abates` |
| `Pred (DetCN every_Det (UseN_CN a_bomb_N)) (Compl abandon_1_V2 (UseN abaca_2_N))` | `cada bomba atómica abandona abacá` | `every A-bomb abandons abaca` |
| `Pred (DetCN the_Det (ModCN (PositA abasic_1_A) (UseN_CN a_bomb_N))) (UseV abate_2_V)` | `la bomba atómica abásica cede` | `the abasic A-bomb abates` |

These tests confirm that the expanded grammar correctly parses and linearizes sentences incorporating determiners, adjectives, and various verb structures.

## 5. Conclusion and Future Work

The expansion of the abstract syntax and the development of the Python translation function represent a significant step towards a more comprehensive and user-friendly GF-based translation system. The system now leverages the rich linguistic information from the official GF WordNet and provides a programmatic interface for translation.

Future work can focus on:

*   **Further Grammar Expansion**: Incorporating more complex sentence structures, such as relative clauses, conjunctions, and different tense/aspect markers.
*   **Error Handling and Robustness**: Improving the parsing capabilities to handle malformed input gracefully and provide more informative error messages.
*   **User Interface**: Developing a more interactive user interface for translation, potentially integrating with web technologies.
*   **WordNet Integration**: Leveraging the `find_word_in_db` function to dynamically suggest words or help construct abstract syntax trees for unknown words.

## References

*   [Grammatical Framework (GF) Official Website](https://www.grammaticalframework.org/)
*   [GF Resource Grammar Library (RGL) GitHub Repository](https://github.com/GrammaticalFramework/gf-rgl)
*   [GF WordNet GitHub Repository](https://github.com/GrammaticalFramework/gf-wordnet)
